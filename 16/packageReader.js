// All methods assumes that binary starts where method should start
const { binaryToNumber } = require("./helpers");

const readVersionNumber = binary => {
    let version = binaryToNumber(`${input[0]}${input[1]}${input[2]}`), ID = 0;
}